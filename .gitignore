# =========================================================
# Terraform directories & state files
# =========================================================
.terraform/
.terraform.lock.hcl

# Terraform state files (sensitive, never commit)
*.tfstate
*.tfstate.*

# Terraform crash logs
crash.log
crash.*.log

# Terraform plans (optional: ignore plan outputs)
# *tfplan*

# Terraform local overrides (not for version control)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform lock info
.terraform.tfstate.lock.info

# =========================================================
# Terraform variables (sensitive)
# =========================================================
*.tfvars
*.tfvars.json

# =========================================================
# Credentials (general security)
# =========================================================
*.json
*.pem
*.key

# =========================================================
# CLI config (local only)
# =========================================================
.terraformrc
terraform.rc

# =========================================================
# System / Misc
# =========================================================
.DS_Store
