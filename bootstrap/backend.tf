# Backend configuration for remote state
# Keep this commented for first run, then uncomment and run: terraform init -migrate-state

# terraform {
#   backend "gcs" {
#     bucket = "your-project-id" # replace with your preferred project ID
#     prefix = "bootstrap"
#   }
# }
